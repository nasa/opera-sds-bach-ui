import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  global: {
    "MuiFormControl-root": {
      display: "flex",
    },
  },
  smallGap: {
    paddingTop: "3.5px",
  },
}));
