import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  global: {
    "MuiFormControl-root": {
      display: "flex",
    },
  },
  selectInput: {
    margin: "5px 0px",
    display: "flex",
    justifyContent: "center",
  },
}));
