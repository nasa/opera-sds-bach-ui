import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  dialogDiv: {
    width: "50%",
  },
  closeBtn: {
    justifyContent: "center",
  },
  scrollableModal: {
    justifyContent: "center",
    textAlign: "center",
  },
}));
