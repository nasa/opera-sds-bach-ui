import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  dialogDiv: {
    width: "50%",
  },
  closeBtn: {
    justifyContent: "center",
  },
  scrollableModal: {
    justifyContent: "center",
    textAlign: "center",
  },
}));
