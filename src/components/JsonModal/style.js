import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  dialogDiv: {
    width: "75%",
  },
  scrollableModal: {
    overflow: "scroll",
  },
  closeBtn: {
    justifyContent: "center",
  },
}));
