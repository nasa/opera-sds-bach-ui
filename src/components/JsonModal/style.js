import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  dialogDiv: {
    width: "75%",
  },
  scrollableModal: {
    overflow: "scroll",
  },
  closeBtn: {
    justifyContent: "center",
  },
}));
