import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  summaryTable: {
    marginBottom: "16px",
  },
  reportTable: {
    minHeight: "400px",
    height: "86%",
  },
}));
