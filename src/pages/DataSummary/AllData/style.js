import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  iconLabel: {
    display: "flex",
    alignItems: "center",
    marginLeft: "16.5px",
  },
  spanLabel: {
    marginLeft: "13px",
  },
}));
